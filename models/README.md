# Models Directory

This directory contains trained model artifacts and related files.

- `sentiment_model.pkl`: Trained sentiment analysis model
- `training_stats.json`: Training statistics and metadata

Models are generated by the data pipeline and consumed by the API service.
